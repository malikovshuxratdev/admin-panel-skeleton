# Copy to `.env` and fill in. Never commit `.env`.
# Do NOT wrap values in quotes — Vite keeps them literally.

VITE_BASE_URI=              # backend API origin
VITE_OAUTH_URI=             # OAuth / SSO origin
VITE_FILE_URI=              # file / object-storage base
VITE_FRONTEND_URI=          # public frontend origin
